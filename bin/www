#!/usr/bin/env node

var app = require('../app');


// Start listening for HTTP requests
var server = app.listen(app.get('port'), function () {
    var host = server.address().address;
    var port = server.address().port;

    console.de('app listerning at http://%s:%s', host, port);
});